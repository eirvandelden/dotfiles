# Review: ai-native-workflow-phase-4-finish

Context for every round: the repository has no `REVIEW.md` or `REVIEW.local.md` at its root, so the passes follow the defaults in `claude/.claude/skills/new-repo-setup/references/REVIEW.md`. This branch has no `spec.md` or `plan.md` in its own change folder (this file creates the folder). Compliance is measured against `docs/changes/ai-native-workflow/phases/04-finish.md` plus the parent `spec.md` and `plan.md`.

## Round 1 — 2026-09-23T09:03Z — 91ece029

Suite state: `change_scope_test.rb`, `fill_pr_template_test.rb`, `consent_guard_test.rb`, `skill_parity_test.rb` and `change_folder_test.rb` are green. RuboCop was not run (no Gemfile at the repository root). Security pass: nothing found. The allowlist stays outside the repository, and `change-scope` passes no user input to a shell.

Compliance: every file the phase names changed as described. Nothing unplanned was added. No existing test was weakened, skipped or deleted. `consent_guard_test.rb` is green after the extraction. Acceptance criteria: plain template → `test_a_plain_template_gets_its_matching_headings_filled`; checkbox template → `test_a_checkbox_style_template_gets_its_matching_headings_filled_too`; no template → `test_no_template_produces_a_context_block_from_intent_and_plan_alone`; no matching headings → `test_a_template_with_no_matching_headings_gets_a_context_block_prepended`; unknown headings keep their text → only partly proven (see finding 3); scope own / allowlisted / unknown / no remote → the four `ChangeScopeTest` tests.

- [x] Important: A test heading that starts with "How" gets the plan's Files and Order of work, not its Proof. `CATEGORIES` is checked in order, and `/implementation|how|details/i` comes before `/test|verify/i`. Reproduced: a `## How has this been tested?` heading received `F1`/`W1`, and `PROOF1` appeared nowhere. "How has this been tested?" and "How to test" are common PR template headings. — `claude/.claude/skills/finish/scripts/fill-pr-template:8` → fixed (Match test/verify headings before implementation/how in fill-pr-template)
- [x] Important: The skill calls its scripts by paths relative to the dotfiles repository: `claude/.claude/skills/plan/scripts/change-folder`, `git/.config/git/worktree-tools/review-report-fresh`, `claude/.claude/skills/finish/scripts/change-scope` and `.../fill-pr-template`. In a work checkout, which is the scope with the most steps, none of these paths exist. The installed paths (`~/.claude/skills/...`, `~/.config/git/worktree-tools/review-report-fresh`, as `lefthook.yml:274` and `spec/SKILL.md:15` already use) are not given. — `claude/.claude/skills/finish/SKILL.md:27` → fixed (Use installed script paths in finish/SKILL.md, Use the installed skill path in implement, plan, spec, reviewer, Regenerate the reviewer Codex agent for the installed skill path, Guard against repo-relative script paths leaking into skill bodies — finish/SKILL.md, implement/plan/spec/reviewer.md, regenerated Codex agent, and a skill_parity_test.rb guard against the regression)
- [x] Important: The spec criterion "unknown headings keep their template text" is proven only through the fallback path. `test_an_unmatched_heading_keeps_its_template_text` uses the same no-match template as the Context-block test, so the template is appended whole. No test runs the `"#{section[:heading]}#{section[:body]}"` branch, where an unmatched heading sits among matched ones. — `test/fill_pr_template_test.rb:109` → fixed (Prove an unmatched heading keeps its text among matched ones)
- [x] Nit: `HEADING` splits on every line that starts with `#`, including lines inside a fenced code block. Reproduced: a `# run this` line inside a ```` ```sh ```` block under `## Summary` was treated as a heading. Filling Summary then removed the opening fence and left an unclosed ```` ``` ```` in the PR body. — `claude/.claude/skills/finish/scripts/fill-pr-template:15` → fixed (Skip fenced code blocks when splitting PR template headings)
- [x] Nit: A filled section ends with a single `\n`, so the next heading follows the filled text without a blank line (`O1\n## How…`). Two newlines keep the output consistent with the template's own spacing. — `claude/.claude/skills/finish/scripts/fill-pr-template:78` → fixed (End a filled PR template section with a blank line)
- [x] Nit: The spec says "offer the last-used list". The skill keeps the last-used list only within one session and otherwise falls back to `~/.claude/finish/reviewers`. Across sessions, the last list someone actually confirmed is lost. — `claude/.claude/skills/finish/SKILL.md:77` → fixed (finish creates and opens the PR in both scopes, not just work — persists to `~/.claude/finish/last-reviewers`, offered when `reviewers` is absent or "last used" is picked)
- [x] Nit: `assert_not` is defined again in both new test files, and `assert_no_match` reimplements Minitest's own `refute`/`refute_match`. — `test/fill_pr_template_test.rb:157` → dismissed: attempted with plain Minitest `refute`/`refute_match` in both files; verified reproducible in an isolated `test/` dir that the shared `rubocop/.rubocop.yml`'s `Rails/RefuteMethods` cop (Include: `**/test/**/*`) fires regardless of Rails inheritance and `rubocop -a` (run by the pre-commit hook) silently rewrites them back to `assert_not`/`assert_no_match`; removing the local helpers under that autocorrect leaves the file calling undefined methods. No disable comment and no shared-config change (out of scope, broad blast radius) — the helpers stay, as the finding itself anticipated ("if it fires, keep refute_* and say so").
- [x] Nit: The branch has no `docs/changes/<slug>/intent.md`, `spec.md` or `plan.md`, which rule 17 expects. The review round has to borrow the parent change's phase document. — `docs/changes/ai-native-workflow-phase-4-finish/` → dismissed: phase of the umbrella change, artifacts live in docs/changes/ai-native-workflow/

2 more nits not listed.

## Round 2 — 2026-09-23T12:19Z — a6f5b9af

Suite state: `change_scope_test.rb`, `fill_pr_template_test.rb`, `consent_guard_test.rb`, `skill_parity_test.rb` and `change_folder_test.rb` are green. `change-scope` still finds `remote_matcher.rb` when it is run through a symlinked file or a symlinked skill directory (`require_relative` resolves the real path), so the stowed Claude path and the Codex `agents` link both work. Security pass: nothing found. Every Round 1 finding is closed or dismissed.

Compliance: the "both scopes open the PR" decision (finish creates and opens the PR in both scopes, not just work) is applied the same way in the skill, the phase document, the parent `spec.md` and `habits.md`, with the exceptions in the nits below. No existing test was weakened, skipped or deleted.

- [x] Important: A "What type of change is this?" heading is filled with the Summary text, and its checkbox list is removed. `/summary|what/i` matches any heading that contains "what". Reproduced: a template with `## Description` and `## What type of change is this?` (`- [ ] Bug fix`, `- [ ] Feature`) left Description with its placeholder text and replaced the checkbox list with `PROB`/`OUT`. "Type of change" and "Description" are common PR template headings. The pattern comes from the phase document, so the fix needs a decision there too: for example, match "what" only as a whole heading word followed by the change ("What does this PR do", "What changed"), and add "description" to the summary pattern. — `claude/.claude/skills/finish/scripts/fill-pr-template:9` → fixed (Match summary headings by content, not the bare word "what")
- [x] Important: §5 reads the PR title from `intent.md` after §4 deleted it. `gh pr create --title "<the title after "# Intent:" in intent.md>"` runs after `git rm -r docs/changes/<slug>`, so an agent that follows the steps literally finds no file. Record the title in §3, next to the filled body. — `claude/.claude/skills/finish/SKILL.md:71` → fixed (Record the PR title and body file before finish deletes the change folder)
- [x] Nit: A sub-heading under a matched heading gets the same fill a second time. Reproduced: `## Testing` followed by `### Steps to verify` printed `PROOF1` under both. Only the top matched heading should be filled; its sub-headings should go with it. — `claude/.claude/skills/finish/scripts/fill-pr-template:91` → fixed (Match summary headings by content, not the bare word "what")
- [x] Nit: §3.3 says to "hold the filled body" and write nothing, but `--body-file` needs a file. The skill does not say where that file goes. A file in the working tree would be untracked after §4, or could be committed by accident. Name a path outside the repository (for example a `mktemp` file), or use `--body-file -` with the body on stdin. — `claude/.claude/skills/finish/SKILL.md:56` → fixed (Record the PR title and body file before finish deletes the change folder)
- [x] Nit: Two descriptions still say that only work fills the PR body and pushes. `SKILLS-INDEX.md` says "and at work fills the PR template, asks about an ADR, pushes, and requests reviewers". `agents/openai.yaml` says "and at work the PR handoff". The header comment of `fill-pr-template` says "Fills a work PR template". — `SKILLS-INDEX.md:49` → fixed (Match summary headings by content, not the bare word "what", Fix descriptions that still said only work fills and pushes the PR — the fill-pr-template header comment was updated alongside the matching-engine fix; SKILLS-INDEX.md and openai.yaml separately)
- [x] Nit: The phase document's Verification still expects the personal run to print "the merge command, pushes nothing". Since the 2026-09-23 decision, the personal run pushes and opens the PR. — `docs/changes/ai-native-workflow/phases/04-finish.md:48` → fixed (Update the finish phase document for the Round 2 fixes)

## Round 3 — 2026-09-23T12:37Z — a4e57190

Suite state: `change_scope_test.rb`, `fill_pr_template_test.rb`, `consent_guard_test.rb`, `skill_parity_test.rb` and `change_folder_test.rb` are green. RuboCop was not run. Security pass: nothing new. The builtin personal pattern `github\.com[:/]eirvandelden/` has no host anchor, so `https://example.org/github.com/eirvandelden/x` also counts as personal. This code existed before the branch and was only moved into `remote_matcher.rb`, so it is not listed as a finding.

Compliance: the Round 2 fixes match the phase document's updated Step 1 (summary pattern, "type" exclusion, choice lists, sub-heading absorption) and Step 3 (title and body recorded before the folder is removed). Each fix has a test. No existing test was weakened, skipped or deleted.

- [x] Important: A checkbox list under a sub-heading of a matched heading is deleted. The absorption from Round 2 removes every sub-heading of a filled section, including sub-sections that are choice lists. Reproduced: `## Description` followed by `### Type of change` (`- [ ] Bug fix`, `- [ ] Feature`) gave `## Description` with `PROB`/`OUT` and no checkbox list. The phase document says a choice-list section "is never filled" and "keeps its template text". Stop the absorption at a sub-section whose body is a choice list, and keep that sub-section. Add a test for this case. — `claude/.claude/skills/finish/scripts/fill-pr-template:116` → fixed (Keep a choice-list sub-section when absorbing a matched heading)
- [x] Nit: The category patterns have no word boundaries. Reproduced: `## Screenshots / showcase` received the plan's Files and Order of work (`how` in "showcase"), and `## Latest release notes` received the Proof (`test` in "Latest"). The user sees the result in §3.2, which limits the damage. `\b` around each word fixes it. — `claude/.claude/skills/finish/scripts/fill-pr-template:11` → fixed (Anchor PR template category patterns to word starts)
- [x] Nit: When some heading matches but the test heading is a choice list, the plan's Proof is in the PR body nowhere. Reproduced: `## Description` plus `## Testing` with `- [ ] Unit tests added` gave the checkboxes and no `PROOF1`. The Context block is added only when no heading matches. This follows the phase document, so any change needs a decision there first. — `claude/.claude/skills/finish/scripts/fill-pr-template:109` → fixed (Append unfilled categories at the end instead of one Context block)
- [x] Nit: §5.4 does not say what to do when `after-push` exits non-zero. Only §7 says "print stderr and stop before requesting reviewers". An agent that follows §5 and §6 in order continues to §6. Put the stop rule in §5.4 too. — `claude/.claude/skills/finish/SKILL.md:77` → fixed (State the after-push stop rule in §5.4 too)
- [x] Nit: The "fixed (…)" SHAs in Rounds 1 and 2 do not point at commits on the branch. The branch was rebased after those rounds, so `9f4ac9db`, `f83c0dd0`, `e838ad88`, `f4f2800d`, `a1cb154d` and `6b795e6b` are not ancestors of HEAD. The Round 2 matching fix, for example, is now `f8d15f2b`. Update them, or name the commit subjects, which do not change on a rebase. — `docs/changes/ai-native-workflow-phase-4-finish/review.md:13` → fixed (Rewrite review.md's fixed marks as commit subjects, not SHAs)
