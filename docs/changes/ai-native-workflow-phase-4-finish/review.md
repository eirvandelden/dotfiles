# Review: ai-native-workflow-phase-4-finish

Context for every round: the repository has no `REVIEW.md` or `REVIEW.local.md` at its root, so the passes follow the defaults in `claude/.claude/skills/new-repo-setup/references/REVIEW.md`. This branch has no `spec.md` or `plan.md` in its own change folder (this file creates the folder). Compliance is measured against `docs/changes/ai-native-workflow/phases/04-finish.md` plus the parent `spec.md` and `plan.md`.

## Round 1 — 2026-09-23T09:03Z — 91ece029

Suite state: `change_scope_test.rb`, `fill_pr_template_test.rb`, `consent_guard_test.rb`, `skill_parity_test.rb` and `change_folder_test.rb` are green. RuboCop was not run (no Gemfile at the repository root). Security pass: nothing found. The allowlist stays outside the repository, and `change-scope` passes no user input to a shell.

Compliance: every file the phase names changed as described. Nothing unplanned was added. No existing test was weakened, skipped or deleted. `consent_guard_test.rb` is green after the extraction. Acceptance criteria: plain template → `test_a_plain_template_gets_its_matching_headings_filled`; checkbox template → `test_a_checkbox_style_template_gets_its_matching_headings_filled_too`; no template → `test_no_template_produces_a_context_block_from_intent_and_plan_alone`; no matching headings → `test_a_template_with_no_matching_headings_gets_a_context_block_prepended`; unknown headings keep their text → only partly proven (see finding 3); scope own / allowlisted / unknown / no remote → the four `ChangeScopeTest` tests.

- [x] Important: A test heading that starts with "How" gets the plan's Files and Order of work, not its Proof. `CATEGORIES` is checked in order, and `/implementation|how|details/i` comes before `/test|verify/i`. Reproduced: a `## How has this been tested?` heading received `F1`/`W1`, and `PROOF1` appeared nowhere. "How has this been tested?" and "How to test" are common PR template headings. — `claude/.claude/skills/finish/scripts/fill-pr-template:8` → fixed (a1cb154d)
- [x] Important: The skill calls its scripts by paths relative to the dotfiles repository: `claude/.claude/skills/plan/scripts/change-folder`, `git/.config/git/worktree-tools/review-report-fresh`, `claude/.claude/skills/finish/scripts/change-scope` and `.../fill-pr-template`. In a work checkout, which is the scope with the most steps, none of these paths exist. The installed paths (`~/.claude/skills/...`, `~/.config/git/worktree-tools/review-report-fresh`, as `lefthook.yml:274` and `spec/SKILL.md:15` already use) are not given. — `claude/.claude/skills/finish/SKILL.md:27` → fixed (0e519ea1, fa67dc50, d7f80c03, e2b3c5e5 — finish/SKILL.md, implement/plan/spec/reviewer.md, regenerated Codex agent, and a skill_parity_test.rb guard against the regression)
- [x] Important: The spec criterion "unknown headings keep their template text" is proven only through the fallback path. `test_an_unmatched_heading_keeps_its_template_text` uses the same no-match template as the Context-block test, so the template is appended whole. No test runs the `"#{section[:heading]}#{section[:body]}"` branch, where an unmatched heading sits among matched ones. — `test/fill_pr_template_test.rb:109` → fixed (f5407267)
- [x] Nit: `HEADING` splits on every line that starts with `#`, including lines inside a fenced code block. Reproduced: a `# run this` line inside a ```` ```sh ```` block under `## Summary` was treated as a heading. Filling Summary then removed the opening fence and left an unclosed ```` ``` ```` in the PR body. — `claude/.claude/skills/finish/scripts/fill-pr-template:15` → fixed (c08e0824)
- [x] Nit: A filled section ends with a single `\n`, so the next heading follows the filled text without a blank line (`O1\n## How…`). Two newlines keep the output consistent with the template's own spacing. — `claude/.claude/skills/finish/scripts/fill-pr-template:78` → fixed (5ffa8b0f)
- [x] Nit: The spec says "offer the last-used list". The skill keeps the last-used list only within one session and otherwise falls back to `~/.claude/finish/reviewers`. Across sessions, the last list someone actually confirmed is lost. — `claude/.claude/skills/finish/SKILL.md:77` → fixed (6b795e6b — persists to `~/.claude/finish/last-reviewers`, offered when `reviewers` is absent or "last used" is picked)
- [x] Nit: `assert_not` is defined again in both new test files, and `assert_no_match` reimplements Minitest's own `refute`/`refute_match`. — `test/fill_pr_template_test.rb:157` → dismissed: attempted with plain Minitest `refute`/`refute_match` in both files; verified reproducible in an isolated `test/` dir that the shared `rubocop/.rubocop.yml`'s `Rails/RefuteMethods` cop (Include: `**/test/**/*`) fires regardless of Rails inheritance and `rubocop -a` (run by the pre-commit hook) silently rewrites them back to `assert_not`/`assert_no_match`; removing the local helpers under that autocorrect leaves the file calling undefined methods. No disable comment and no shared-config change (out of scope, broad blast radius) — the helpers stay, as the finding itself anticipated ("if it fires, keep refute_* and say so").
- [x] Nit: The branch has no `docs/changes/<slug>/intent.md`, `spec.md` or `plan.md`, which rule 17 expects. The review round has to borrow the parent change's phase document. — `docs/changes/ai-native-workflow-phase-4-finish/` → dismissed: phase of the umbrella change, artifacts live in docs/changes/ai-native-workflow/

2 more nits not listed.
