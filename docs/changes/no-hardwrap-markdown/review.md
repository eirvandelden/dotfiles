# Review: no-hardwrap-markdown

No `REVIEW.md` or `REVIEW.local.md` at the repository root; the reviewer's default passes (Bugs, Security, Compliance) were used. Suite state at review time: `test/no_hardwrap_rule_test.rb`, `test/markdown_hardwrap_hook_test.rb`, `test/markdown_rule_mirror_test.rb` and `test/lefthook_local_hooks_test.rb` all green, markdownlint-cli 0.48.0.

## Round 1 — 2026-09-23T10:23:36Z — 2185abe4

- [x] Important: Unwrap mode deletes text. `textOnLine` keeps only the first child token on each continuation line, so everything after the first inline element is lost when the line is deleted. `one two\nthree *bold* four \`code\` five` fixes to `one two three ` (the rest of line 2 is gone); `> line one\n> line two *b* c` fixes to `> line one line two `. Phase 2 runs this fix over every dotfiles-owned `.md` file, so it would silently destroy content. Continuation text needs to be the whole line after its prefix, not one token. — `markdownlint/.config/markdownlint/no-hardwrap.cjs:62` → fixed (999ef214)
- [x] Important: Unwrap mode deletes an indented continuation line entirely. For `para one\n   indented two`, the first child on line 2 is a `linePrefix` token (not in `INDENT_OR_PREFIX_TYPES`), so the inserted text is whitespace and the words are lost: the result is `para one    `. — `markdownlint/.config/markdownlint/no-hardwrap.cjs:9` → fixed (999ef214)
- [x] Important: A soft wrap inside an inline span is not reported. `isWrapped` only looks at the paragraph's direct children, so a `lineEnding` inside emphasis, a link label or code span is missed: `one *bold\nspan* rest` and `see [a link\ntext](http://x) end` produce no finding and are not fixed. This breaks requirement 5 ("reports each paragraph … whose text runs over more than one line") and would leave such paragraphs in place after phase 2 (criterion 17). — `markdownlint/.config/markdownlint/no-hardwrap.cjs:26` → fixed (999ef214)
- [x] Important: The fix tests only use plain-word continuation lines, so none of the three defects above is caught. Criteria 10 and 11 are proven only for text without inline markup, indentation or a block quote; there is no block-quote fix test at all, although the plan's Risks section names block quotes as covered by one. — `test/no_hardwrap_rule_test.rb:104` → fixed (999ef214)
- [x] Nit: The new playbook rule is itself hardwrapped over two lines, and says the rule applies even in a hardwrapped file. The pre-commit check flags it; agents reading `agents.md` see a wrapped example of the rule. Same for the edited sentence in `repository-layout.md`. — `agents.md:272` → fixed (309a13ad)
- [x] Nit: In report mode, a paragraph with a hard break followed by a soft wrap says `join lines 1–3 into one line`, which tells the agent to remove the intentional hard break too. The unwrap mode already splits by runs; the report detail could use the same runs. — `markdownlint/.config/markdownlint/no-hardwrap.cjs:74` → fixed (999ef214)
- [x] Nit: The Claude hook checks only `.md`; `.markdown` and upper-case `.MD` files are skipped without notice. — `claude/.claude/hooks/markdown-hardwrap.rb:38` → fixed (2820cd24)
- [x] Nit: `setup_lint_bin_dirs` assumes `node` sits in the same directory as `cspell`. Where it does not (for example a version-manager shim layout), the symlink is dangling and the existing cspell commit tests start failing, where before this change the whole cspell directory was on `PATH`. — `test/lefthook_local_hooks_test.rb:177` → fixed (2820cd24)
- [x] Nit: `test_missing_markdownlint_tells_claude_nothing` builds `PATH` from Ruby's directory; if Ruby and markdownlint share a directory the hook finds markdownlint and the test fails for a reason unrelated to the hook. — `test/markdown_hardwrap_hook_test.rb:82` → fixed (2820cd24)

Security: nothing found. The hook passes the file path to markdownlint as an argument without a shell; the rule file is loaded from `~/.config/markdownlint`, which the user owns.

Compliance: every file the plan names changed as described; nothing unplanned. Acceptance criteria 1–2 (`test/markdown_rule_mirror_test.rb`), 4–11 (`test/no_hardwrap_rule_test.rb`), 12–14 (`test/lefthook_local_hooks_test.rb`) and 15–16 (`test/markdown_hardwrap_hook_test.rb`) each have the test the plan's Proof names, and all exist. Criterion 3 checked by hand: no `no-hardwrap` file or `MEMORY.md` line in the dotfiles memory directory. Criterion 17 is phase 2. No existing test weakened, skipped or deleted; `repo_env` and `git` in `test/lefthook_local_hooks_test.rb` gained a keyword argument with the old behaviour as default.

## Round 2 — 2026-09-24T11:57:49Z — 3bb92ac9

All round 1 findings verified fixed. Suite state: `test/no_hardwrap_rule_test.rb` (18 runs), `test/markdown_hardwrap_hook_test.rb` (7), `test/markdown_rule_mirror_test.rb` (2) and `test/lefthook_local_hooks_test.rb` (9) green, markdownlint-cli 0.48.0. Probed by hand and correct: lazy list and quote continuation, nested lists, ordered lists, `> >` and `> -` nesting, backslash and trailing-space hard breaks, wraps inside code spans, inline HTML, angle-bracket links, link destinations, image alt text, CRLF line endings; underlined headings and reference definitions are not reported.

- [ ] Important: Unwrap mode replaces the text of a multi-line inline HTML comment with dots. markdownlint masks HTML comment content in `params.lines`, and `joinedContinuation` builds the inserted text from `params.lines`. `x <!-- one\ntwo three --> y` fixes to `x <!-- one ... ..... --> y`. The words are gone and nothing reports it. The fix command is what the Claude hook tells agents to run in any repository. Block-level comments (a line starting with `<!--`) are HTML blocks and are not affected, so no dotfiles-owned file hits this today. Build continuation text from the unmasked source (the token text, or the file read from `params.name`), or skip the fix for a paragraph whose run contains `htmlText`; add a fix test with a comment spanning lines. — `markdownlint/.config/markdownlint/no-hardwrap.cjs:80`
- [ ] Nit: Unwrapping a footnote definition keeps the continuation indentation: `[^1]: note one\n    note two` fixes to `[^1]: note one     note two` (five spaces). The footnote indentation token is not in `NOT_LINE_TEXT_TYPES`. It renders the same, but leaves a double space the round 1 fix aimed to remove. — `markdownlint/.config/markdownlint/no-hardwrap.cjs:9`
- [ ] Nit: The commit check globs `*.md` while the Claude hook now also checks `.markdown`. A staged `.markdown` file gets no commit warning. The spec says `.md` for the commit check, so this is consistent with the spec but not with the hook. — `lefthook.yml:49`
- [ ] Nit: The fix command in the hook message puts the file path in unquoted. A path with a space gives Claude a command that fails when copied. — `claude/.claude/hooks/markdown-hardwrap.rb:33`

Security: nothing found. Same as round 1; the new `.markdown` match and the fix changes do not add a shell or a new input.

Compliance: round 1 fixes stay inside the planned files. The plan's Proof lists the round 1 tests and each exists and passes. Nothing unplanned changed. Criterion 3 still holds (no `no-hardwrap` memory entry). No test weakened, skipped or deleted since round 1.
