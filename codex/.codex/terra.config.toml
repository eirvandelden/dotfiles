# Planning and research profile: codex -p terra
# Layers over ~/.codex/config.toml, which stays on Luna for implementing.
model = "gpt-5.6-terra"
model_reasoning_effort = "xhigh"

[tui]
screen_reader_detection_done = true
